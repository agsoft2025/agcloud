apiVersion: v1
kind: Service
metadata:
  name: {{ include "agcloud-backend.fullname" . }}
  labels:
    {{- include "agcloud-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agcloud-backend.selectorLabels" . | nindent 4 }}
