agcloud-backend has been deployed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}

Check rollout status:
  kubectl rollout status deployment/{{ include "agcloud-backend.fullname" . }} -n {{ .Release.Namespace }}

Check readiness (spec §7.4):
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"

{{- if not .Values.secrets.create }}

secrets.create is false — this release expects an externally-managed Secret
named "{{ include "agcloud-backend.secretName" . }}" to already exist in this
namespace with the keys listed in values.yaml's secrets.data.
{{- else }}

secrets.create is true — a Secret was rendered from values.yaml's
secrets.data. Confirm you did NOT commit real values there; for anything
beyond local/staging use, switch to an externally-managed secret instead
(see the SECRETS WARNING at the top of values.yaml).
{{- end }}

{{- if not .Values.ingress.enabled }}

No Ingress is enabled. Reach the service from inside the cluster at:
  http://{{ include "agcloud-backend.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
{{- end }}
